export interface Post{
    boardIndex: number,
    boardTitle: string,
    userId: string,
    boardDate: string,
    boardContent: string,
    memberCount: number,
    members: string[],
    isRecruiting: boolean,
};
